---
import { Image } from 'astro:assets';
const { blok } = Astro.props;
---

<section class="py-16 sm:py-24">
  <div class="container">
    <div class="flex flex-col">
      <h1
        class="mb-5 sm:mb-6 text-3xl sm:text-6xl leding-[1.1] text-neutral-950 font-heading">
        {blok.titulo}
      </h1>
      <p
        class="text-lg sm:text-[28px] sm:leading-9 text-neutral-600 max-w-[35em]">
        {blok.subtitulo}
      </p>
    </div>
    <div
      class="mt-8 overflow-hidden rounded-[15px] sm:rounded-md shadow-sm aspect-video">
      <Image
        src={blok.imagen_hero.filename}
        alt={blok.imagen_hero.alt ||
          'Proyecto Terra Regia Residencial'}
        format="webp"
        inferSize
        class="w-full h-full object-cover"
      />
    </div>
  </div>
</section>
