---
import { Image } from 'astro:assets';
import heroImg from '@/assets/hero-image.png';
import { DropdownNav } from './dropdown-nav';
import { DropdownNavMobile } from './dropdown-nav-mobile';
---

<section>
  <div class="container pt-10 sm:pt-16">
    <div class="">
      <h1
        class="font-heading text-4xl tracking-tight sm:text-[80px] sm:leading-none text-balance max-w-[50rem]">
        Conoce nuestros proyectos de casas y terrenos residenciales
      </h1>
      <p class="mt-6 text-lg sm:text-xl text-blue-600 max-w-[35em]">
        Encuentra tu espacio ideal para vivir en las mejores ubicaciones del área metropolitana de
        Monterrey
      </p>
      <div
        class="relative mt-4 sm:mt-12 rounded-[15px] sm:rounded-lg overflow-hidden shadow-sm sm:aspect-video">
        <Image
          src={heroImg}
          alt="Imagen de familia"
          format="webp"
          width="1300"
          height="629"
          class="w-full h-full object-cover"
          loading="eager"
        />
        <div
          class="hidden sm:absolute sm:top-6 sm:right-6 sm:flex sm:items-center sm:justify-center">
          <DropdownNav client:load />
        </div>
      </div>

      <div class="sm:hidden mt-8">
        <DropdownNavMobile client:load />
      </div>
    </div>
  </div>
</section>
