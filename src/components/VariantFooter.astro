---
const {items} = Astro.props
---

<div class="flex flex-row gap-4 justify-center">
  {items.map((item:string) => (
    <p class="border border-black px-4 py-2 rounded-full font-medium">{item}</p>
  ))}
</div>