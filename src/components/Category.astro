---
import {Link} from '@/components/link'
import {cn, sluglify} from '@/lib/utils'
const {name = "Todos",  activeCategory} = Astro.props
const currentPath = Astro.url.pathname
const unsluglifyNameCategory = sluglify(name.toLowerCase())
console.log(unsluglifyNameCategory, activeCategory)
const isActive = activeCategory?.toLowerCase() === unsluglifyNameCategory || (currentPath === '/tipo-de-propiedad' && name === 'Todos')

---


<Link href={name === 'Todos' ? '/tipo-de-propiedad' : sluglify(`/tipo-de-propiedad/${unsluglifyNameCategory}`)} variant="invertedOutline" className={cn(`w-44 border border-white bg-transparent text-white`, isActive && `bg-white text-black border border-white`)}>{name}</Link>