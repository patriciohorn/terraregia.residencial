---
import ProjectCard from './ProjectCard.astro';
const { projects = 'Todos', activeLocation } = Astro.props;
const locations = projects.map((project: any) => project.data.location);
console.log('Locations:', activeLocation);
---

<section>
  <div class="grid md:grid-cols-2 gap-x-12 gap-y-8 sm:gap-y-12">
    {
      projects.map(async (project: any, idx: string) => (
        <ProjectCard project={project} badge={true} button={true} />
      ))
    }
  </div>
</section>
